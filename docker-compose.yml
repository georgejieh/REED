services:
  backend:
    build: ./backend
    ports:
      - "8000:7860"
    env_file:
      - ./backend/.env
    volumes:
      - ./backend/settings.yaml:/app/settings.yaml:ro
      - ./data/digests:/app/data/digests
