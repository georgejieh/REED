services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - ./data:/data
    environment:
      REED_RUNTIME_MODE: local
      REED_DATABASE_PATH: /data/reed.db
      REED_ALLOWED_HOSTS: 127.0.0.1:8000,localhost:8000
      REED_LOCAL_ALLOWED_ORIGINS: http://127.0.0.1:8000,http://localhost:8000
