# REED public-tree audit 2026-07-23

Final audit of the public tree at HEAD `c921dce` (chunk 6a+6d). The next
chunk landed is 7a (README update) and 7b (this audit).

## Method

Ran the following probes against the committed tree.

### Tracked-tree fingerprint scan

```
git ls-files | xargs grep -lEn \
  'subagent|delegation|Hermes|Sebastian|georgejieh|\.hermes|AGENTS\.md|CLAUDE\.md|\.claude|\.codex|cursorrules|check_gt\.py|Fable|Opus|Codex|Claude Code|Chunk [0-9]|TODO|FIXME'
```

Hits:

- `backend/app/market_data/stooq.py:44`: HTTP `User-Agent` literal
  `REED/0.1 (+https://github.com/georgejieh/REED)`.
- `backend/app/providers/tools.py:146`: same `User-Agent` literal.

Both are project-controlled outbound identification, not agent
identity. Allowed under the project's anti-fingerprint doctrine (the
ban is on the operator's personal identity, not on the public repo
URL string used in HTTP headers).

### Em-dash scan

```
git ls-files | xargs grep -lP '\xe2\x80\x94'
```

Zero hits. Tracked text is em-dash free.

### Emoji scan

```
git ls-files | xargs grep -lP '[\x{1F300}-\x{1FAFF}]'
```

Zero hits.

### Tracked test files

```
git ls-files | grep -E '(^|/)(tests?/|__tests__/|/test_|.*_test\.|.*\.test\.|/conftest\.|pytest\.ini)'
```

Zero hits. The project policy is "no tracked test files" and the
policy holds.

### Line ending policy

| Path | Required | Observed |
|------|----------|----------|
| `backend/**/*.py` | CRLF | CRLF (all committed Python is CRLF, including `pyproject.toml`, `uv.lock`, `.env.example`) |
| `backend/Dockerfile` | LF | LF |
| `docker-compose.yml` | LF | LF |
| `dashboard/**/*.{ts,tsx,css,json,md,html}` | LF | LF (0 non-LF files) |

The CRLF baseline on backend Python is a long-standing project rule
applied at `git add` time by `core.autocrlf=true` on the operator's
machine. New backend files are committed CRLF; new dashboard files are
committed LF. The audit verifies the rule is intact.

### Tracked-tree junk

```
git status --short
```

Expected: only the `M README.md` from chunk 7a. No `.tmp_*.mjs` probe
files left behind from chunk 6c. No `node_modules/`, no `dist/`, no
`package-lock.json`, no `__pycache__/`, no `.env*`. Clean.

## Verdict

APPROVED. The public tree at `c921dcec` is clean by every project rule.
The fingerprint hits are project-controlled HTTP `User-Agent` strings,
sanctioned by the project's anti-fingerprint doctrine.

## What this chunk ships

- This audit file.
- A README update that points operators at `docs/HF_DEPLOYMENT.md` and
  `scripts/deploy_vps.sh` and explains the dashboard's two build modes.

The next action after this chunk is the QMD re-embed so the local QMD
index reflects the new REED artifacts.